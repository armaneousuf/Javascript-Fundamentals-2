//Take four parameters. Multiply the four numbers and then return the result

function multiply(x, y, z, a) {
  let mult = 1;
  return x * y * z * a;
}

let mult = multiply(2, 4, 6, 8);
console.log(mult);
