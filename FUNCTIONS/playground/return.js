function tenTimes(number) {
    const result = number * 10;
    return result;
}

const output = tenTimes(25);
console.log(output);

function cutHalf(number) {
    const half = number / 2;
    return half;
}

const outputOfHalf = cutHalf(3.333);
console.log(outputOfHalf);